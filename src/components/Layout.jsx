import { Outlet } from "react-router-dom";

/**
 * @description The layout for the whole app
 * @author [Hoang Le Chau](https://github.com/hoanglechau)
 */
const Layout = () => {
  return <Outlet />;
};

export default Layout;
